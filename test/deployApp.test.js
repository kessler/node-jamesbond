var should = require('should')
var deployApp = require('../lib/deployApp.js')
var path = require('path')

describe('deployApp', function () {
	
	// it('does not deploy to existing directories', function (done) {
	// 	var app = {
	// 		paths: [
	// 			path.resolve(__dirname, '..', '..')
	// 		]
	// 	}

	// 	deployApp(app, function(err) {
	// 		err.should.be.an.Error
	// 		err.message.should.eql('directory exists')
	// 		done()
	// 	})
	// })

})